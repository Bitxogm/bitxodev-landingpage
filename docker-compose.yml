
version: '3.8'
services:
  portfolio:
    build: .
    ports:
      - "80:80"
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.portfolio.rule=Host(`yourdomain.com`)"
